west-commands:
  - file: scripts/lf_build.py
    commands:
      - name: lf-build
        class: LfBuild
        help: Invoke lfc and then west build
      - name: lf-fed-build
        class: LfFedBuild
        help: Invoke lfc and then west build, tweaked for federated
      - name: lf-fed-flash
        class: LfFedFlash
        help: Flash LF program binaries for different federation topology modes
