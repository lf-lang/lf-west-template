/*
* Simple Hello-world. Will work with any Zephyr-supported platform
*/
target C {
    platform: "Zephyr",
    single-threaded: true
};

main reactor {
    reaction(startup) {=
        printf("Hello World\n");
    =}
}

